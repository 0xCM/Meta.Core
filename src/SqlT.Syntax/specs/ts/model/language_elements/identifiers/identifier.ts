class identifier {

    constructor(value : string, quoted : boolean)
    {
        this.value = value;
        this.quoted = quoted;        
    }
    
    readonly value : string
    readonly quoted : boolean
}
