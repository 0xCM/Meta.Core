<Project  ToolsVersion="15.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <PropertyGroup>
    <DevArea>sqlt</DevArea>
    <AssemblyName>SqlT.Db.Test</AssemblyName>
    <OutputType>EXE</OutputType>
    <AutoGenerateBindingRedirects>False</AutoGenerateBindingRedirects>
    <DevToolDir>$(DevTools)\</DevToolDir>
    <TopDir>$(DevAreaRoot)\SqlT\</TopDir>
    <TopDevDir>$(TopDir)</TopDevDir>
    <TopConfigDir>$(TopDevDir)src\SqlT.Build\</TopConfigDir>
  </PropertyGroup>
  
  <Import Project="$(TopConfigDir)Build.Common.props"/>
  <Import Project="$(common-distdir)markdig.refs.props"/>
  <Import Project="$(common-distdir)rabbitmq.refs.props"/>
  <Import Project="$(msca-distdir)msca.refs.props"/>
  <Import Project="$(sys-distdir)sys.refs.props"/>
  <Import Project="$(msdac-distdir)msdac.refs.props"/>
  <Import Project="$(metacore-distdir)metacore.refs.props"/>
  <Import Project="$(msbuild-distdir)msbuild.refs.props"/>
  <Import Project="$(MSBuildToolsPath)\Microsoft.CSharp.targets" />
  <Import Project="$(TopConfigDir)Build.Exe.Post.props"/>

</Project>
