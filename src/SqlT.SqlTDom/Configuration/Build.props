<Project ToolsVersion="15.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <PropertyGroup>
    <DevArea>sqlt</DevArea>
    <AssemblyName>SqlT.SqlTDom</AssemblyName>
    <OutputType>Library</OutputType>
    <TopDir>$(DevAreaRoot)\$(DevArea)\</TopDir>
    <TopDevDir>$(TopDir)</TopDevDir>
    <TopConfigDir>$(TopDevDir)src\SqlT.Build\</TopConfigDir>
  </PropertyGroup>

  <Import Project="$(TopConfigDir)Build.Common.props"/>
  <Import Project="$(sys-distdir)sys.refs.minimal.props"/>
  <Import Project="$(sys-distdir)sys.refs.filesystem.props"/>
  <Import Project="$(metacore-distdir)metacore.refs.props"/>
  <Import Project="$(msdac-distdir)msdac.refs.props"/>
  <Import Project="$(MSBuildToolsPath)\Microsoft.CSharp.targets" />
  <Import Project="$(TopConfigDir)Build.Lib.Post.props"/>


</Project>
