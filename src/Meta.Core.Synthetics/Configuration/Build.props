<Project ToolsVersion="15.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <PropertyGroup>
    <DevArea>MetaCore</DevArea>
    <AssemblyName>Meta.Core.Synthetics</AssemblyName>
    <TargetName>$(AssemblyName)</TargetName>
    <TopDir>$(DevAreaRoot)\$(DevArea)\</TopDir>
    <TopDevDir>$(TopDir)</TopDevDir>
    <TopConfigDir>$(TopDir).config\</TopConfigDir>
  </PropertyGroup>

  <Import Project="$(TopConfigDir)Build.Lib.props"/>
  <Import Project="$(common-distdir)json.refs.props"/>
  <Import Project="$(common-distdir)math.refs.props"/>
  <Import Project="$(common-distdir)fare.refs.props"/>
  

</Project>